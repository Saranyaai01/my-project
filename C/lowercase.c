#include<stdio.h>
    int main()
    {
        char upr[50];
        printf("Enter the upper case string:");
        get(upr);
        printf("/n The lower case string is: %s",strlwr(upr));
        return 0;
    }
        

    
